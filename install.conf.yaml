- defaults:
    link:
      relink: true

- clean: ["~", "~/.config"]

- link:
    ~/.config/omf: "omf"

    ~/.gemrc: "gemrc"
    ~/.gitconfig: "gitconfig"

- shell:
  - [git submodule update --init --recursive, Installing submodules]

