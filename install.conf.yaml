- defaults:
    link:
      relink: true

- clean: ["~", "~/.config"]

- shell:
  - [git submodule update --init --recursive, Installing submodules]

- link:
    ~/.config/omf: "omf"
    ~/.gemrc: "gemrc"
    ~/.gitconfig: "gitconfig"
    ~/.vim: "vim"
    ~/.vimrc: "vimrc"
    ~/.z.sh: "z/z.sh"

