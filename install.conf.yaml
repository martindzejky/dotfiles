- defaults:
    link:
      relink: true

- clean: ["~"]

- link:
    ~/.gemrc: ".gemrc"
    ~/.gitconfig: ".gitconfig"
    ~/.vimrc: ".vimrc"

- shell:
  - [git submodule update --init --recursive, Installing submodules]

