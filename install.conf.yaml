- defaults:
    link:
      relink: true

- clean: ["~"]

- link:
    ~/.gemrc: ".gemrc"
    ~/.gitconfig: ".gitconfig"

- shell:
  - [git submodule update --init --recursive, Installing submodules]

