- defaults:
    link:
      relink: true

- clean: ["~", "~/.config"]

- shell:
  - [git submodule update --init --recursive, Installing submodules]

- link:
    ~/.config/omf: "omf"
    ~/.gemrc: "gemrc"
    ~/.gitconfig: "gitconfig"
    ~/.z.sh: "z/z.sh"

